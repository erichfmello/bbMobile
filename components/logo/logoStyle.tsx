import { StyleSheet } from "react-native";

const LogoStyle = StyleSheet.create({
    logoContent: {
        alignContent: "center",
        alignItems: "center",
    },
    logoImage: {
        width: 200,
        height: 200,
    }
});

export default LogoStyle;