import { StyleSheet } from 'react-native';

import '../../assets/css/variables.scss';

const AppStyle = StyleSheet.create({
    screen: {
        flex: 1,
        // backgroundColor: $backgroudGeneral,
        alignItems: 'center',
        justifyContent: 'center',
    },
});

export default AppStyle;